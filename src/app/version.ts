const previousVersions = ['v1.0.1', 'v1.1.0', 'v2.1.0'];
const currentVersion = 'v2.2.0';
const allVersions = [...previousVersions, currentVersion];
export const LOCAL_STORAGE_VERSION_KEY = currentVersion;
